import { AnnouncementTextPipe } from './announcement-text.pipe';

describe('AnnouncementTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementTextPipe();
    expect(pipe).toBeTruthy();
  });
});
